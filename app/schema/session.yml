session:
  fields:
    - name: sess_id
      type: binary
      attributes:
        length: 128

    - name: sess_data
      type: blob

    - name: sess_time
      type: integer

    - name: sess_lifetime
      type: integer
      attributes:
        - unsigned

  primary_key:
    - sess_id
