session:
  fields:
    - name: sess_id
      type: string
      attributes:
        length: 255

    - name: sess_data
      type: text
      attributes:
        length: 65535

    - name: sess_time
      type: integer

    - name: sess_lifetime
      type: integer

  primary_key:
    - sess_id
